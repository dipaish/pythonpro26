"""
Quick Start: Click Terminal > New Terminal | pwd (check location) | cd part4 (if needed)

Most common character

Task: Please write a function named most_common_character, which takes a string as its argument. The function returns the character which has the most occurrences within the string. If there are multiple characters with equally many occurrences, the one which appears first in the string should be returned.

first_string = "abcdbde"
print(most_common_character(first_string))

second_string = "exemplaryelementary"
print(most_common_character(second_string))

Sample output:
b
e
"""

# TODO: Write your solution below this line

# Save your file and run it using: python 33_most_common_character.py
# Check: python grade_part4.py

# Commit and push changes to GitHub:
# 1. git add .                                    (stage all changes)
# 2. git commit -m "Completed task 4.5.2"         (commit with message)
# 3. git push                                      (push to GitHub)
